import { createStyles } from '@mantine/core';

export const useStyles = createStyles((theme) => ({
  burger: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: '30px',
    alignSelf: 'right',
    right: 0,
    opacity: 1,
  },
}));
